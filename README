Marx is a simple load-balancing distributed batch job scheduler that uses a benchmark scores 
injected into binary tree to load balance scheduled jobs accross a network of nodes.
Marx is created in C and very light weight.

DEPENDENCIES : Libconfig
